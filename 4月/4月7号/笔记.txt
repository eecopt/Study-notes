linux：
下载地址：http//www.kernel.org/
内核版本分为三个部分：主版本号、次版本号、末版本号
次版本号中基数为开发般版偶数为稳定版
发行版：redhat、fedora、centos、unbuntu、debian
python:
列表：[1,23,4]
字典：{1，2，3}
元组：（1，23，4）
集合：[1、23、4、]
五大数据类型：
number（数字）
数字类型 ：
1、int(符号整形)2、long（长整形）
3、float(浮点型)4、complex（复数）
list（列表）：
列表可以完成大多数集合类的数据结构实现。它支持字符，数字，字符串甚至可以包含列表（所谓嵌套）
string（字符串）：
字符串或串(String)是由数字、字母、下划线组成的一串字符
tuple（元组）：
类似于List（列表），内部元素用逗号隔开。但是元组不能二次赋值
dictionary(字典)：

