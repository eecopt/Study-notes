比较
大于/小于：a > b，a < b。
大于/小于或等于：a >= b，a <= b。
等于：（a == b请注意双等号=。单个符号a = b表示分配）。
不等于。在数学中，记号是≠，但在JavaScript中，它被写为前面带有感叹号的赋值a != b。

布尔是结果
与其他运算符一样，比较返回一个值。在这种情况下，该值为布尔值
true-表示"s是"，“正确”或“真相”
false-表示"否"，"错误"或"不是事实"。
